#ifndef VECTOR
#define VECTOR
#include <vector>
#endif

using namespace std;

vector<vector<float> *>* Derivative(vector<vector<float> *>*); 
float  StandardDeviation(vector<vector<float> *>*);
float Mean(vector<float>*);
//vector<vector<bool> *>* BinaryThreshold(vector<vector<float> *>*);
//vector<vector<float> *>* InvertSign(vector<vector<float>* >*);
//vector<vector<bool> *>* HitOrMiss(vector<vector<bool> *>*);
//vector<vector<bool> *>* Erode(vector<vector<bool>*>*,vector<vector<bool>>);


